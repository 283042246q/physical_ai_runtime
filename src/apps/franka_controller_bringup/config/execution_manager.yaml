# One pose route for the single-arm FR3 effort impedance pipeline.
execution_manager:
  ros__parameters:
    joint_names: "fr3_joint1,fr3_joint2,fr3_joint3,fr3_joint4,fr3_joint5,fr3_joint6,fr3_joint7"

    source_namespace: "/action_sources"
    sources: >-
      {"marker":{"priority":100,
                 "inactive_timeout_s":0.5,
                 "pose_contracts":["pose_target"]}}

    pose_output_topic: "/task_space_joint_impedance_controller/pose_reference"
    status_topic: "/execution_manager/status"

    status_rate_hz: 2.0
    stale_timeout_s: 0.5
    max_future_s: 0.1
